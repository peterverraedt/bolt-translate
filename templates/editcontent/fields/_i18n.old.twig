{% set localefieldvals = context.content.get(contentkey) %}
{% set locales = app['config'].get('general/locales') %}

{% for locale, info in locales %}
    {{ include('@bolt/editcontent/fields/_i18n-locale.old.twig', {'content': localefieldvals, 'locale': locale, 'locale_slug': info.slug, 'locale_label': info.label}) }}
{% endfor %}
